/** Armazon de las pantallas publicas: sin panel, sin navegacion, centradas. */
export default function SesionLayout({ children }: { children: React.ReactNode }) {
  return <div className="acceso">{children}</div>
}
